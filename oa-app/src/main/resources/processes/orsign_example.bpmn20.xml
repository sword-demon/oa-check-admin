<?xml version="1.0" encoding="UTF-8"?>
<definitions xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
             xmlns:flowable="http://flowable.org/bpmn"
             targetNamespace="http://oa.admin.com/approval">

    <!-- Or-sign (或签) example: any one assigned user can approve -->
    <process id="orsign_example" name="Or-sign Example" isExecutable="true">
        <startEvent id="start" name="Start"/>
        <sequenceFlow id="flow1" sourceRef="start" targetRef="orsignTask"/>

        <userTask id="orsignTask" name="Or-sign Approval"
                  flowable:assignee="${initiator}">
            <multiInstanceLoopCharacteristics isSequential="false"
                flowable:collection="assigneeList" flowable:elementVariable="assignee">
                <completionCondition>${nrOfCompletedInstances == 1}</completionCondition>
            </multiInstanceLoopCharacteristics>
        </userTask>
        <sequenceFlow id="flow2" sourceRef="orsignTask" targetRef="exclusiveGw"/>

        <exclusiveGateway id="exclusiveGw" name="Approved?"/>
        <sequenceFlow id="flow3" sourceRef="exclusiveGw" targetRef="endApproved">
            <conditionExpression>${approved == true}</conditionExpression>
        </sequenceFlow>
        <sequenceFlow id="flow4" sourceRef="exclusiveGw" targetRef="endRejected">
            <conditionExpression>${approved == false}</conditionExpression>
        </sequenceFlow>

        <endEvent id="endApproved" name="Approved"/>
        <endEvent id="endRejected" name="Rejected"/>
    </process>
</definitions>
