# Design: 审批流后台权限管理平台模版 (混合路线)

Generated by /office-hours on 2026-05-14
Branch: main
Repo: oa-check-admin
Status: APPROVED
Mode: Builder

## Problem Statement

Builder 需要一套可复用的后台管理平台模版, 支持 RBAC 权限管理 + 审批流引擎, 用于后续新项目直接套用. 目标是 fork 后跑一条 `init.sh` 脚本就能得到一套完整的权限+审批底座, 30 分钟内跑起来.

## What Makes This Cool

不是"又一个后台模版", 而是一套 **审批基础设施脚手架**: 新项目不用从零搭建权限和审批, 直接 fork → 改包名 → 跑起来. 后续演进为代码生成器 (Approval-as-Code).

## Constraints

- 技术栈: Java (Spring Boot 3.x) 后端, Vue 3 前端 (React 适配作为 Phase 3 长期考虑, 不在 Phase 1/2 范围内)
- 审批引擎: Flowable (BPMN 2.0)
- 前端流程设计器: Phase 2 实现, 基于 bpmn-js 深度定制, 钉钉风格
- 数据库: MySQL (国内生态更成熟)
- 权限框架: Sa-Token (轻量, 比 Spring Security 更容易掌控)
- 交付节奏: 分步走, Phase 1 先能跑再迭代

## Premises

1. **核心价值是开箱即用** — 一条命令得到 RBAC + 审批链, 不需要手动配置半天
2. **Flowable 作为审批引擎** — BPMN 2.0 标准, 生态成熟, 后续可扩展
3. **前端设计器需要钉钉级 UX** — bpmn-js 默认面板不够, 需要深度定制
4. **分步交付** — Phase 1 = RBAC + 简单串行审批链, Phase 2 = 可视化设计器 + 条件分支

## Cross-Model Perspective

Codex (GPT-5.4) 独立审查建议:

- **最酷版本**: Approval-as-Code 生成器 — 用 YAML/DSL 定义业务实体和审批流程, 自动生成全套代码. 比模板仓库更强大, 是"平台编译器"而非"模板".
- **50% 现成方案**: JHipster 已有脚手架编译器能力 (JDL 驱动), 但审批侧几乎为空白. Flowable 是审批引擎半成品. 核心工作是做"产品化胶水".
- **周末 MVP**: Java 21 + Spring Boot 3.x + PostgreSQL + Flyway + Spring Security + JWT + Flowable OSS, 只做串行审批链.

Claude 的不同意见: JHipster 生成代码量巨大, 魔改成本比从零写更高, 与"自己掌控"目标冲突. Sa-Token 比 Spring Security 更轻量可控. MySQL 比 PostgreSQL 在国内 Java 生态更成熟.

## Landscape Findings (技术生态搜索)

### 主流方案 (Layer 1)
- **Flowable Engine** (9,260 Star) + **bpmn-js** (9,531 Star) — 行业标准组合
- **RuoYi-Vue-Plus** (dromara, 2,192 Star) — Sa-Token + MyBatis Plus + Vue 3, 活跃维护
- **bpmn-process-designer** (miyuesc, 1,784 Star) — Vue 3 + bpmn-js, Flowable 深度适配

### 新兴方案 (Layer 2)
- **Warm-Flow** (dromara, 513 Star) — 不依赖 Flowable 的轻量国产引擎, 中国式审批
- **FlowLong** (飞龙, 775 Star) — JSON 格式模型, 仿钉钉设计器
- **vue-bpmn-designer** (tsai996) — Vue 3 + Vite 6 + TS, 技术栈最新但 Star 极少

### 被忽视的好方案 (Layer 3)
- **RuoYi-Flowable** (tony2y, 668 Star) — RuoYi + Flowable 最成熟集成
- **wansenai/flow** (837 Star) — Vue 3 + TS, 表单+流程+门户三合一
- **ding-flowable-vue2-enhance** — 聚合多个前端设计器 + Flowable 后端的"全家桶"

## Approaches Considered

### Approach A: 最小可用内核
Spring Boot 3.x + MySQL + Sa-Token + Flowable + Vue 3. fork 后手动改包名. RBAC 五表模型 + 简单串行审批链.
- Effort: S (1-2 周)
- Risk: 低
- Pros: 快, 代码量可控, 能立刻在真实项目里用
- Cons: 每次新项目需手动改包名和基础配置, 复用效率有上限

### Approach B: Approval-as-Code 生成器
CLI 工具 + YAML/DSL, 自动生成 Spring Boot 模块 + Flowable 流程定义 + 前端页面 + 数据库迁移.
- Effort: L (1-2 个月)
- Risk: 中
- Pros: 真正的一条命令, 生成器可持续迭代, 复用性最高
- Cons: 生成器本身开发和维护成本高, 初期 ROI 不如模板仓库

### Approach C: 混合路线 (已选定)
模板仓库 + `init.sh` 脚本自动替换包名/端口/数据库名. 后续逐步升级为轻量代码生成器. 从 A 演进到 B.
- Effort: S+ (2-3 周起步)
- Risk: 低
- Pros: 既快又有演进路径, 不一步到位但方向对
- Cons: 中间态可能让代码组织变复杂

## Recommended Approach: 混合路线 (方案 C)

### Phase 1: 模板仓库 + 初始化脚本 (2-3 周)

**后端:**
- Java 21 + Spring Boot 3.x
- MySQL 8.x + MyBatis Plus (或 MyBatis-Flex)
- Sa-Token (权限认证, 比 Spring Security 更轻量)
- Flowable 7.x OSS (审批引擎)
- Flyway (数据库版本管理)
- Hutool (工具类)

**前端:**
- Vue 3 + TypeScript + Vite
- Element Plus (UI 组件库)
- Pinia (状态管理)
- VueRouter (路由)
- Axios (HTTP 客户端)

**数据模型 (ER 关键字段):**

```
sys_user
  id, username, password_hash, nickname, email, phone,
  dept_id (FK -> sys_dept), status, created_at, updated_at, deleted

sys_dept (部门/组织, 审批流需要"部门经理"角色)
  id, parent_id, dept_name, sort, leader_user_id, status, deleted

sys_role
  id, role_name, role_key, sort, data_scope (1=全部 2=本部门 3=自定义),
  status, deleted

sys_permission
  id, parent_id, permission_name, permission_type (1=菜单 2=按钮 3=API),
  path, component, icon, sort, status, deleted

sys_user_role
  id, user_id (FK), role_id (FK)

sys_role_permission
  id, role_id (FK), permission_id (FK)

sys_role_dept (data_scope=3 时的自定义数据范围)
  id, role_id (FK), dept_id (FK)
```

```
biz_process_template (审批流程模板)
  id, template_name, template_key (唯一标识, 如 "leave_request"),
  flowable_process_definition_id (关联 Flowable 的 ProcessDefinition),
  form_config (JSON, 表单字段定义, 示例: `{"fields":[{"name":"leave_days","type":"number","label":"请假天数","required":true},{"name":"reason","type":"textarea","label":"请假原因","required":true}]}`),
  version, status, deleted

biz_approval_instance (审批实例)
  id, process_template_id (FK), instance_title,
  flowable_process_instance_id (关联 Flowable 的 ProcessInstance),
  initiator_user_id (FK -> sys_user),
  status (1=审批中 2=通过 3=驳回 4=撤回 5=终止),
  form_data (JSON, 提交的表单数据),
  created_at, updated_at, end_at

biz_approval_task (审批任务)
  id, approval_instance_id (FK),
  flowable_task_id (关联 Flowable 的 Task),
  assignee_user_id (FK -> sys_user),
  task_name (节点名称, 如 "部门经理审批"),
  task_result (1=通过 2=驳回 3=转办),
  task_comment (审批意见),
  created_at, completed_at

biz_approval_cc (抄送记录)
  id, approval_instance_id (FK), cc_user_id (FK -> sys_user),
  cc_reason, read_at, created_at

biz_audit_log (审计日志)
  id, user_id, module, action, target_type, target_id,
  detail (JSON), ip, created_at
```

**Flowable 映射策略 (关键架构决策):**

Flowable 为 source of truth (流程状态), 业务表为查询优化层.

| 操作 | Flowable 侧 | 业务表侧 | 一致性保证 |
|------|-------------|---------|-----------|
| 发起审批 | `runtimeService.startProcessInstanceById()` | 插入 `biz_approval_instance` + `biz_approval_task` | 同一事务 (Flowable 配置 Spring 事务管理器共享 DataSource) |
| 审批通过/驳回 | `taskService.complete()` | 更新 `biz_approval_task` + `biz_approval_instance` | Flowable 同步 Listener (配置在 BPMN XML 的 TaskListener 中, 在 Flowable 事务提交前同步触发, 确保同事务一致性) |
| 流程结束 | Flowable `processInstanceEnded` 事件 | 更新 `biz_approval_instance.status = 2` | Flowable 全局事件监听器 (同步执行, 同事务) |

Flowable 的 `ACT_*` 表不直接暴露给前端. 所有查询走业务表, Flowable 仅负责流程编排和状态推进. 这样前端不需要了解 Flowable 的表结构.

**Flowable 审批人识别策略:**
- 不同步用户数据到 Flowable 的 `ACT_ID_USER` 表
- 流程定义中使用 Flowable 的 `initiator` 变量记录发起人
- 审批人通过自定义 `TaskListener` 在节点创建时从业务表读取 assignee (模板中硬编码的 userId 列表)
- `taskService.setAssignee(taskId, userId)` 直接使用 `sys_user.id` 作为 Flowable 的 assignee 标识
- Phase 2 加入条件分支后, 通过 Flowable 的 UEL 表达式 (如 `${initiatorDept.managerId}`) 动态计算审批人

**Flyway 与 Flowable 表初始化分工:**
- Flowable 使用 `database-schema-update: true` 自管 `ACT_*` 表的创建和升级
- Flyway 仅管理业务表 (`sys_*` 和 `biz_*`) 的 DDL 和种子数据
- 两者不交叉, 不存在冲突

**审批能力 (Phase 1):**
- 发起 / 待办 / 通过 / 驳回 / 撤回
- 串行审批链 + 抄送
- 固定审批人指定

**串行审批链用户流程 (Phase 1 边界):**
1. 发起人选择审批模板 → 填写表单 → 提交
2. 系统按模板预设的审批人顺序 (如: 组长 → 经理 → HR), 依次生成待办任务
3. 每个审批人: 打开待办 → 查看表单 → 通过/驳回 (附审批意见) → 下一个审批人收到待办
4. 驳回: 直接退回发起人, 流程结束, 发起人可修改后重新提交
5. 撤回: 仅限发起人在第一个审批人未处理时撤回, 已处理的节点不可撤回
6. 抄送: 流程结束后, 系统自动抄送给模板中配置的抄送人列表
7. 固定审批人: 在模板中硬编码 userId 列表, Phase 1 不支持从组织架构动态选择

**`init.sh` 脚本功能:**
- 交互式输入: groupId, artifactId, packageName, 端口, 数据库名
- 自动替换所有包名、端口、数据库名 (使用自定义 shell 脚本 + envsubst 模板变量替换, 不依赖 Python/Cookiecutter, 仅需 bash)
- 自动生成 docker-compose.yml (含 MySQL health check + Flowable 初始化等待)
- 跑完后 `docker compose up` 即可启动
- 自动创建超级管理员账号 (admin/admin123, 首次登录强制改密码)
- Flyway 自动初始化 RBAC 种子数据 + 示例审批流程模板

**init.sh 前置依赖:**
- macOS / Linux (Windows 通过 WSL2)
- Java 21+ (JAVA_HOME 配置正确)
- Maven 3.8+ 或 Gradle 8+
- Node.js 18+ (前端构建)
- Docker + Docker Compose
- "30 分钟" 不含首次下载 Docker 镜像时间

**不做的 (Phase 1 明确跳过):**
- 可视化流程设计器
- 条件分支 / 并行网关 / 会签 / 或签
- 低代码表单设计器
- React 适配层
- 多租户
- 消息通知 (钉钉/企微/飞书)

### Phase 2: 可视化设计器 + 条件分支 (Phase 1 完成后)

**前端设计器选型 (按优先级):**
1. **bpmn-process-designer** (miyuesc) — 1,784 Star, Vue 3 + Flowable 深度适配, 最成熟
2. **自研基于 bpmn-js** — 如果 miyuesc 的设计器不满足钉钉风格要求, 基于 bpmn-js 深度定制
3. **参考 ding-flowable-vue2-enhance** — 作为中国式审批流实现的参考

**新增审批能力:**
- 条件分支 (如金额 > 5000 走总监)
- 并行网关 / 排他网关
- 会签 / 或签
- 动态候选人

### Phase 3: 代码生成器演进 (长期)

- 从 `init.sh` 升级为 CLI 工具
- 支持 YAML/DSL 定义业务实体和审批流程
- 自动生成: Entity + Mapper + Service + Controller + 前端页面 + 数据库迁移
- 参考 JHipster 的 JDL 思路, 但专注于审批+权限领域

## Open Questions

- 前端 UI 框架: Element Plus 还是 Naive UI? (Element Plus 生态更大, Naive UI 更轻量)
- ORM 选型: MyBatis Plus 还是 MyBatis-Flex? (Plus 生态成熟, Flex 更现代)
- 是否需要内置用户导入/导出 (Excel) 功能?

## API 风格规范

- RESTful, 统一前缀 `/api/v1/`
- 统一响应体: `{ "code": 200, "msg": "success", "data": {...}, "timestamp": 1234567890 }`
- 分页: `{ "code": 200, "data": { "list": [...], "total": 100, "page": 1, "pageSize": 20 } }`
- 错误码: 4 位数, 前两位模块 (10=系统, 20=权限, 30=审批), 后两位具体错误
- 认证: Sa-Token, Header 携带 `satoken: {token_value}`
- 所有写操作返回变更后的完整对象, 前端不需要额外查询

## Sa-Token 集成范围 (Phase 1)

- 登录认证 (账号密码 + 记住我)
- 权限校验 (接口级 `@SaCheckPermission`, 角色级 `@SaCheckRole`)
- 会话管理 (单设备登录, 踢人下线)
- 不做: 单点登录 (SSO), OAuth2, 多端互踢 (Phase 1 不需要)

## 前端页面规划 (Phase 1)

```
/login                          登录页
/dashboard                      仪表盘 (待办数量统计, 近期审批动态)
/system/user                    用户管理 (CRUD + 分配角色)
/system/role                    角色管理 (CRUD + 分配权限)
/system/permission              权限管理 (菜单树 + 按钮/API 权限)
/system/dept                    部门管理 (树形结构)
/approval/template              审批模板管理 (CRUD + 配置审批链)
/approval/template/create       创建审批模板 (表单设计 + 审批人配置)
/approval/my-apply              我的申请 (发起 + 查看)
/approval/my-apply/create/:id   发起审批 (填写表单)
/approval/my-todo               我的待办 (审批/驳回)
/approval/my-done               我的已办 (历史记录)
/approval/cc                    抄送给我的
```

## 技术验证点 (Phase 1 首先要做的)

1. **Flowable 7.x + Spring Boot 3.x 兼容性验证** — 确认版本组合能跑通, 确认 Flowable 7.x 的 API 变化不影响 Phase 1 的功能
2. **Flowable + Spring 事务管理器共享 DataSource** — 这是"Flowable 为 source of truth"策略的前提, 需要验证事务能正确回滚
3. **Flowable 全局事件监听器** — 验证能通过监听器同步更新业务表

## Success Criteria

Phase 1 完成标准:
- [ ] `git clone` → `./init.sh` → `docker compose up` 全程不超过 30 分钟 (不含首次下载依赖和 Docker 镜像)
- [ ] 能登录, 能创建用户/角色/权限
- [ ] 能发起一个"请假审批"示例流程
- [ ] 审批人能在待办列表中通过/驳回
- [ ] 审批记录可查, 有审计日志
- [ ] 代码结构清晰, 模块化, 方便扩展

## Distribution Plan

- GitHub 仓库, MIT 协议
- README 包含一键启动说明
- `init.sh` 脚本自动配置
- Docker Compose 一键部署
- 后续考虑发布到 Maven Central (如果做代码生成器)

## Next Steps

1. 搭建项目骨架: Spring Boot 3.x + Vue 3 + Docker Compose
2. 实现 RBAC 五表模型 + Sa-Token 集成
3. 集成 Flowable, 实现串行审批链
4. 做一个"请假审批"示例流程
5. 编写 `init.sh` 初始化脚本
6. 编写 README + 一键启动文档

## What I noticed about how you think

- 你说"有参考但想自己掌控" — 这不是 NIH (Not Invented Here) 综合征, 你已经看过了市面上的方案, 做出了有依据的判断. 这说明你关心的是代码的所有权和可维护性, 而不是重复造轮子.
- 你选了"分步走先能跑再迭代"而不是"一步到位做完整" — 这个判断在流程设计器这种复杂度很高的模块上尤其正确. 先用简单审批链验证数据模型和引擎集成, 再加可视化, 避免了"做了设计器但数据模型不对要推翻重来"的陷阱.
- 你对 Flowable + BPMN + 钉钉设计器这个组合有明确的预期 — 这说明你对这个领域有实际使用经验, 不是在想象需求.

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 1 | PASS | SCOPE EXPANSION, 5 expansions accepted, 0 unresolved |
| Codex Review | `/codex-plan-review` | Independent 2nd opinion | 1 | ISSUES_FOUND | 10 findings, 1 cross-model tension resolved |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | PASS | 11 issues resolved, 0 open |
| Design Review | `/plan-design-review` | UI/UX gaps | 1 | PASS | 7 passes completed, 3/10 → 7.5/10 |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

**CROSS-MODEL:** Codex outside voice ran in both Eng Review and CEO Review. Key tensions: (1) Flowable may be over-engineered for Phase 1's fixed serial chain — resolved: keep Flowable (user chose A in both D10/Eng and D10/CEO). (2) Template versioning was blank — resolved: add version management design.

**UNRESOLVED:** 0

**VERDICT:** CEO + Eng + Design Reviews all PASS. All decisions resolved. Ready to implement.

### Eng Review Decisions (D1-D11)

| ID | Topic | Decision |
|----|-------|----------|
| D1 | SqlSessionFactory conflict | Add resolution strategy to design doc |
| D2 | Sa-Token security | Lock to v1.44.0 + JSON serialization + CVE mitigation |
| D3 | Flowable async | Disable async executor in Phase 1 |
| D4 | Version pinning | Spring Boot 3.5.x + Flowable 7.2.0 + Sa-Token 1.44.0 + Java 21 + MySQL 8.x + MyBatis-Plus 3.5.x |
| D5 | CI/CD | Skip for now, add later. Record Flowable 7.x OSS UI removal |
| D6 | init.sh idempotency | Add `.init-done` marker + `--force` flag |
| D7 | ORM + UI framework | MyBatis-Plus + Element Plus |
| D8 | Test strategy | Full test plan: unit + integration + E2E |
| D9 | Performance notes | Document JSON query risk + N+1 risk |
| D10 | Flowable necessity | Keep Flowable + add dual-write reconciliation |
| D11 | Template versioning | Add version management design (published templates immutable) |

### Design Review Decisions (D1-D4)

| ID | Topic | Decision |
|----|-------|----------|
| D1 | Review scope | Full 7-pass review |
| D2 | Layout | Sidebar layout (Element Plus el-container + el-aside + el-menu) |
| D3 | Interaction states | Full state table for all pages |
| D4 | User journeys | 3 key journeys: first login, first approval submit, approval processing |

### Design Review Scores

| Pass | Topic | Before | After |
|------|-------|--------|-------|
| 0 | Initial completeness | 3/10 | — |
| 1 | Information Architecture | 3/10 | 7/10 |
| 2 | Interaction State Coverage | 1/10 | 7/10 |
| 3 | User Journey | 2/10 | 9/10 |
| 4 | AI Slop Risk | 4/10 | 7/10 |
| 5 | Design System Alignment | 2/10 | 6/10 |
| 6 | Responsive & Accessibility | 1/10 | 5/10 |
| 7 | Unresolved Decisions | 3/10 | 9/10 |

### Design System Spec (Phase 1)

- Layout: Sidebar 220px (collapsed 64px), top nav 60px, content padding 20px
- Color: Element Plus default (#409EFF) + status colors (pass green, reject red, pending blue, withdrawn gray)
- Table: Zebra stripe, fixed header, action column right-pinned, pagination bottom-right
- Form: Label 100px, required red asterisk, validation below field
- Status tags: 审批中(blue) / 通过(green) / 驳回(red) / 撤回(gray) / 终止(black)
- Browser: Chrome/Edge/Firefox 90+, min width 1280px
- Responsive: Sidebar collapsible for 1024-1280px, no mobile support

### Dashboard Spec

- 4 stat cards: 待办数 / 已办数 / 申请中 / 抄送未读
- Recent activity table: 5 rows, showing latest approval actions with timestamp

### User Management Spec

- Table columns: 用户名 / 昵称 / 部门 / 角色 / 状态 / 操作
- Search: 用户名 (fuzzy) + 状态 (dropdown)
- Create dialog: 用户名 / 昵称 / 密码 / 邮箱 / 手机号 / 部门(select tree) / 角色(multi-select)

### Approval Template Config Spec

- Step form: Basic info → Form field design → Approver configuration
- Approver config: Select users from list + drag to reorder

### Approval Detail Page Spec

- Left: Read-only form data rendered from form_config JSON
- Right: Approval history timeline (vertical, showing each node result + comment + timestamp)

### Locked Version Matrix

| Component | Version | Notes |
|-----------|---------|-------|
| Java | 21 | LTS |
| Spring Boot | 3.5.x | Aligns with Flowable 7.2.0 |
| Flowable | 7.2.0 | Latest OSS stable, no UI/modeler |
| Sa-Token | 1.44.0 | Last stable before Boot 4 refactor, use `sa-token-spring-boot3-starter` |
| MyBatis-Plus | 3.5.x | Mature Flowable integration patterns |
| MySQL | 8.x | — |
| Vue | 3.x | — |
| Element Plus | latest stable | — |
| Vite | 6.x | — |
| Flyway | latest stable | Business tables only |

### Failure Modes

| Failure | Severity | Mitigation |
|---------|----------|------------|
| Flowable async thread loses Sa-Token context | Critical | D3: Disable async in Phase 1 |
| Concurrent withdraw on already-processed task | Critical | Add optimistic locking on approval_task |
| SqlSessionFactory conflict at startup | High | D1: @Primary + @MapperScan isolation |
| Sa-Token deserialization attack (CVE-2025-15222) | High | D2: Use JSON serializer only |
